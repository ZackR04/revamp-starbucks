part of '../../screens.dart';

class QRView extends StatelessWidget {
  const QRView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: 'QR View'.text.makeCentered(),
    );
  }
}
